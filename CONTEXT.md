# CONTEXT · 项目上下文与领域词汇

ai-trip-planner：开源的 ZCode / Claude Code / Codex 插件，用两阶段状态机驱动 agent 端到端产出中文旅行攻略网页站（调研核数 → 成刊 → 部署）。本文件给贡献者一张词汇与架构地图。

## 架构一句话

一个主 skill（端到端流程）+ 十篇分域手册（渐进披露）+ 三套模板（zine 特辑壳 / lite 轻页壳 / trip-state 状态模板）+ 四个脚本（小红书只读搜索 ×2 / 高德路线渲染 / staging 构建）+ 四个命令（薄壳，只指路）。状态持久化在攻略项目目录的 `trip-state.md`。分发面是三清单各认各的宿主：`.zcode-plugin/`（ZCode）、`.claude-plugin/`（Claude Code 市场源，兼作 Codex legacy 市场源）、根 `plugin.json`（Codex 可移植清单）。

## 领域词汇

- **阶段一 · 选方案（发散）**：摸底 → 搜目的地共识 → 每方案一张轻页 → 推荐等拍板。产出决策级材料，不做执行级深调研。
- **阶段二 · 细化（收敛）**：版式样板拍板 → 逐天闭环 → 全站自检 → 部署。拍板是两阶段唯一边界。
- **决策级数据**：够拍板用但不够执行用（车程/住宿可得性/吸引点匹配/门票档/粗预算）。与执行级数据（套餐/时刻表/逐段车程）相对。
- **轻页**：阶段一每方案一张的网页，只装决策级数据 + 目的地共识。拍板后封存。
- **模板壳**：抽掉具体内容、留下版式与共用 CSS 的页面骨架。本仓库三套：`templates/zine`（特辑风攻略站）、`templates/lite`（方案轻页）、`templates/trip-state.template.md`。
- **攻略特辑风（zine）**：杂志式排版：通栏 hero、刊头、衬线大标题、壹贰叁小节号、竖排逐时时间轴、编辑部表格、门票黄框、文末 series 导航。反例：App 壳式 UI（被验收否决过）。
- **软情报 / 硬数据 / 比价 / 事实核验**：逐天深化的四层顺序，口诀「软情报定玩法 → 硬数据定店 → 比价定住宿 → 地图定事实」。软情报=小红书/攻略镜像；硬数据=大众点评；比价=携程/飞猪；地图=高德车程/坐标/路线图。
- **比价通道**：携程/飞猪两家合称，「比价定住宿」的准源。携程=住宿主查口（选店/档期价/房型/取消政策），飞猪=交叉比价与备用深链；附带门票渠道参考与配图源。只读不代订。
- **订房深链**：带城市/日期/店名参数的酒店列表页 URL，家人从攻略页点开直达订房页，下单交还用户。zine 模板住处卡「订房直达」按钮组标配；不需要登录态，未登录降级时照常可出。
- **额度账本**：有上限的动作（小红书搜索、点评进店）随用随记的台账，trip-state.md 的一节。上限是保守风控经验值，不是平台规则。
- **降级矩阵**：setup.md 里「缺某依赖会怎样、降到哪条替代通道」的总表。缺件降级不废流程。
- **增强层**：默认零门槛之上的可选能力（如自配高德 JSAPI key 解锁精细路线图渲染）。
- **只读红线**：不代下单、不代付款、不代发布；登录与验证交用户；触发风控即停。
- **trip-state.md**：攻略项目目录里的状态文件（阶段/需求卡/方案/逐天进度/额度账本/遗留/部署 七节），跨会话续接的唯一事实源。
- **staging 干净副本**：部署前用 `scripts/stage_site.sh` 构建的只含上线文件的副本，绝不携带隐藏目录与本地产物。

## 隐私红线（最高优先级）

本仓库公开。零真实行程信息：模板与示例全部虚构占位；demo 截图一律虚构；建造期票据在 `.scratch/`（gitignore，永不发布）；推送前必须过红线词 grep 审查并经维护者确认。

## 更多

- 架构决策记录见 `docs/adr/`。
- 各域操作知识见 `skills/ai-trip-planner/references/`。
